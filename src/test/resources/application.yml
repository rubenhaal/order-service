spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      enabled: false

order-service:
  welcome-message: "Mensaje de prueba"
  environment: "test"
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics